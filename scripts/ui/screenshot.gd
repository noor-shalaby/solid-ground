extends Node2D


func _ready() -> void:
	Input.mouse_mode = Input.MOUSE_MODE_CAPTURED

# This scene is made for taking screenshots to use as covers and such, instead of using a graphics editor because I'm too lazy lol
